# thesis-types

Shared TypeScript DTOs for the desk and (if Next.js hosts BFF routes) the web server.

**Not scaffolded yet.** When added: Zod schemas that mirror `apps/analysis-api` Pydantic models. No React here.
