# Python (`packages/python`)

Shared library `thesis_platform`: config, DB, Yahoo previous-close parser, native lab LLM adapter, prompt loader, section checks, Storage signed URLs.

Imported by `apps/analysis-api` and `apps/analysis-worker`. No HTTP server here.
