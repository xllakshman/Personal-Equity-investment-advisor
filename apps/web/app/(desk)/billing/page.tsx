import { EmptyState } from "@/components/features/desk/EmptyState";

export default function BillingPage() {
  return (
    <EmptyState
      title="Plans & wallet"
      body="Checkout is a placeholder until a merchant is named. This page does not charge a card."
    />
  );
}
