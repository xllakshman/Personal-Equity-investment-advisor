import { EmptyState } from "@/components/features/desk/EmptyState";

export default function ReportsPage() {
  return (
    <EmptyState
      title="Reports"
      body="Saved notes will list here. Recent names already show on Desk. The note reader is not on this page yet."
    />
  );
}
