# =============================================================================
# Supabase (project https://cmksomahsfmsjufakryw.supabase.co)
# =============================================================================
SUPABASE_URL=https://cmksomahsfmsjufakryw.supabase.co
# Optional — auto-derived from SUPABASE_URL if unset (db.<project-ref>.supabase.co)
SUPABASE_DB_HOST=db.cmksomahsfmsjufakryw.supabase.co
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_DB_PASSWORD=

# =============================================================================
# LLM (worker + analysis-api only — never NEXT_PUBLIC)
# Native labs (D39). HTTP model = model_catalog.provider_model_id.
# openai → api.openai.com · anthropic → api.anthropic.com ·
# xai → api.x.ai · deepseek → api.deepseek.com
# Missing key for the selected row’s provider → job failed / HTTP 503.
# =============================================================================
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=

# =============================================================================
# Market data — previous close only (P4-01 / D40). No key.
# GET query1.finance.yahoo.com/v8/finance/chart/{symbol}?range=5d&interval=1d
# =============================================================================
# Optional override if Yahoo blocks a default User-Agent:
# MARKET_DATA_USER_AGENT=

# =============================================================================
# Analysis API
# =============================================================================
ANALYSIS_API_PORT=8091
